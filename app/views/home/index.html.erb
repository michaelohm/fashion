<% @post.each do |post| %>
  <div>
	  <%= link_to image_tag(post.photo.url(:small)), post_path(post) %><br>
	  Views: <%= post.viewcount %><br>
	  Likes: <%= post.likes.length %><br>
	  Comments: <%= post.comments.length %>
  </div>
<% end %>